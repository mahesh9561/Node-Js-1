<!DOCTYPE html>
<html>
  <head>
    <style>
      /* Styling for the input and button */
      .input-box {
        display: flex;
        gap: 10px;
        margin-bottom: 20px;
      }

      input[type="text"] {
        flex: 1; /* Take up remaining space */
        padding: 12px;
        border: 1px solid #ccc;
        border-radius: 4px;
        box-sizing: border-box;
        font-size: 16px; /* Adjust font size */
      }

      input[type="submit"] {
        width: 100px; /* Set width for button */
        background-color: #4caf50;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: 16px; /* Adjust font size */
        transition: background-color 0.3s ease; /* Smooth transition */
      }
      button {
        background-color: #4caf50;
        color: white;
        padding: 12px 20px;
        margin: 8px 0;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: 16px; /* Adjust font size */
        transition: background-color 0.3s ease; /* Smooth transition */
      }

      button:hover {
        background-color: #45a049;
      }

      /* Styling for the container */
      .button-container {
        display: flex;
        flex-direction: column;
        gap: 10px;
      }

      input[type="submit"]:hover {
        background-color: #45a049;
      }
    </style>
  </head>
  <body>
    <div>
      <h3>Todo App</h3>
      <form action="/create-items" method="post">
        <div class="input-box">
          <input type="text" id="todo" name="todo" placeholder="Your name.." />
          <input class="item1" type="submit" value="Create items" />
        </div>
      </form>
      <div>
        <ul id="item-list" list-group></ul>
      </div>
      <div>
        <form action="/logout" method="post">
          <button>Logout</button>
        </form>
        <form action="/logout_all" method="post">
          <button>Logout from all device</button>
        </form>
      </div>
    </div>
  </body>
</html>
